# Iteration 1 — Query Pack (broad)

See `../AGY_INSTRUCTIONS.md` for command syntax, guardrails, and output conventions.

## Notebook: Business requirement for SW development (`5133ef48-564c-40df-bdd1-142bb7e5bdf3`)

1. What are the standard phases/deliverables an enterprise system-development engagement should complete before system design begins (the requirements/analysis phase), and what artifact does each phase typically produce?
2. What does standard SRS/requirements-engineering practice define as the minimum required content of a "use case and demand analysis" deliverable — what distinguishes a validated use case from a descriptive one?
3. What structure, evidence base, and format does standard practice expect for a "gap analysis" and "recommendations" deliverable?
4. Is "data product inventory" a recognized distinct artifact from a raw "data inventory" in requirements-engineering practice — and if so, what differentiates them?
5. For a project explicitly scoped as requirements/analysis-only (not system design), what is the minimum viable, non-production-ready version of these deliverables that still counts as credible/industry-aligned?

## Notebook: Enterprise Data Architecture (`3adf8897-245c-43c6-aec9-8977f2aab2fb`)

1. What are the standard components of a "Data Management Framework" deliverable (glossary, conceptual data model, governance/stewardship, reference/master data) — is it normal practice to produce these as one integrated framework, or as independent deliverables?
2. What is the standard relationship between a Conceptual Data Model (CDM) and a domain-specific data model (e.g. a Loss & Damage Data Model) — is a domain model typically a specialization within the CDM, or a separate artifact type?
3. What does an enterprise "data product inventory" typically contain, structurally, and how does it differ from a raw data/source inventory?
4. What governance mechanism does standard practice use to keep a business glossary consistent with an evolving conceptual/logical data model as the model is extended?
5. For an early-stage, pre-system-design data blueprint, what can legitimately be deferred to a later phase vs. what must exist even at draft stage in a "data management framework"?
