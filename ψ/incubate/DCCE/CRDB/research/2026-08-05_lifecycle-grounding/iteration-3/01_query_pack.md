# Iteration 3 — Query Pack (clarifying, final round)

See `../AGY_INSTRUCTIONS.md` for command syntax, guardrails, and output conventions.

Context: iteration-2 resolved 6 of 7 open targets. Two remaining items (the LDM/CDM paradigm choice,
and whether DCCE's own ask wording matches the "broad-bundle" Data Management Framework
interpretation) are decisions for the CRDB team, not notebook-answerable, and are being handled
directly with Boss in parallel. This round is deliberately short — 4 questions, not 10 — targeting
only the remaining genuinely clarifiable scoping questions before the final redirection plan is
written.

## Notebook: Enterprise Data Architecture (`3adf8897-245c-43c6-aec9-8977f2aab2fb`)

1. For a pre-system-design data blueprint specifically (not a full operational data platform), which of the DATER framework's 9 dimensions are most critical to assess at draft stage, and which can be safely deferred without weakening the gap analysis's credibility?
2. When an organization commits to a "federated governance plus semantic layer" pattern for a domain model (rather than a strict centralized CDM), what is the minimum semantic-layer artifact needed at draft stage to make that commitment credible rather than merely asserted?

## Notebook: Business requirement for SW development (`5133ef48-564c-40df-bdd1-142bb7e5bdf3`)

1. When a BRD/SRS explicitly states which of several architecture paradigms (centralized vs. federated vs. semantic) a domain model follows, what minimum justification or evidence is expected to accompany that stated choice, at a draft/blueprint stage rather than full system design?
2. Given a 2-week, draft-only engagement, is it standard practice to explicitly scope a reference-data/MDM deliverable to a small number (3-5) of top-priority elements while marking the remainder TBD — and if so, what minimum justification should accompany that scoping decision to keep it credible rather than arbitrary?
