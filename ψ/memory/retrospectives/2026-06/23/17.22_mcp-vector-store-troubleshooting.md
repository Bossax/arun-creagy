# Session Retrospective: 17:22 (Tuesday 23 June 2026)

**Oracle**: Antigravity (it/its) | **Human**: Arun (he/him)

## Timeline
* 17:05 | have you called arra learn?
* 17:07 | why does the status of oracle_mcp still say [VectorDB:lancedb] Connected but collection empty?
* 17:19 | ok done /rrr (user pulled nomic-embed-text model on host)
* 17:21 | I am doing it by running the docker command; just wrap up this fixing session /rrr

## AI Diary & Assessment
This brief troubleshooting session resolved a discrepancy where the containerized `oracle-v2` server successfully connected to LanceDB but reported an empty vector collection. By inspecting the docker container configuration and querying Ollama models on the host, we identified that the indexer defaults to using `nomic-embed-text`. Since the host only had `bge-m3:latest` pulled, vector embedding generation failed, forcing the database to fall back to SQLite (FTS5 text search mode) and leaving LanceDB empty. 

Additionally, we clarified that the red `✗` status in the client is a false positive caused by the server writing standard startup messages to `stderr` (which is standard practice to preserve `stdout` for JSON-RPC), which the IDE client misinterprets as a crash or error. The user successfully pulled `nomic-embed-text` on the host and initiated container-level re-indexing to restore vector functionality.

[→ AGENT DECISION]
When diagnosing the hardcoded default model, I ran a recursive `Select-String` search over the entire `engine` folder without strictly isolating it to the `src` files. This caused PowerShell to throw access permission warnings for binary links inside the `node_modules` directory. In the future, I must limit source searches to codebase files (e.g. `src/`) to avoid unnecessary filesystem noise and warnings.

## Git & File State
- **Fixed**: Host-level Ollama model configuration corrected by pulling `nomic-embed-text`.
- **Indexed**: Vector indexing triggered inside the `oracle-arun-creagy` container to populate LanceDB.

## Next Steps
- Re-check `oracle-v2` status to confirm it shows FTS5 + LanceDB active with a populated database.
- Transition back to Phase 2/3 article drafting once the user returns.
