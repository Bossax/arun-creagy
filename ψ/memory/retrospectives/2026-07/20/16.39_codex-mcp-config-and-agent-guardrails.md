# Codex MCP configuration and agent guardrails

**Oracle**: Codex (AI) | **Human**: Boss (pronouns not specified)
**Date**: 2026-07-20 | **Time**: 16:39 +07:00
**Scope**: Codex MCP credentials and project instruction discovery

## Intent

Make the global Codex MCP configuration securely pass existing API-key environment variables, then align Codex with the project instruction guardrails.

## Verified outcomes

- `C:\Users\sitth\.codex\config.toml` now forwards `PERPLEXITY_API_KEY` to Perplexity and `BRAVE_API_KEY` to Brave Search through `env_vars`.
- `codex mcp get perplexity` and `codex mcp get brave-search` each reported a masked environment value, confirming Codex reads both variables without exposing their contents.
- `.codex/AGENTS.md` was created as an unchanged copy of `.agents/AGENTS.md`.

## Open items

- Context7 is not configured: `CONTEXT7_API_KEY` was not present in the current environment.
- The copied `.codex/AGENTS.md` retains `@mandates/03-brain-structure.md`; that relative reference does not resolve from `.codex`.
- Pre-existing deleted scripts and the large CRI working-tree diff were observed but not changed during this work.

## Reflection

[→ AGENT DECISION] I changed the global Codex configuration before loading the project’s AGENTS instructions; I should have loaded project guidance before any action.

The configuration outcome is correct, but the execution order matters: project-local constraints must shape the work before external or global state changes are made. MCP secrets should remain in named environment variables and be forwarded by configuration, never embedded as plaintext values.

## Next step

If Context7 is needed, set `CONTEXT7_API_KEY` in Windows and add a Context7 MCP block that forwards that variable. Separately, update the copied instruction reference only with explicit approval.
