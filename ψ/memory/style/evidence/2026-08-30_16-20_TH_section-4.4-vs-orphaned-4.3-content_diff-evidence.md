# Diff Evidence — TH / §4.4 (new) vs. orphaned old-§4.4 content (archived as §4.3)

**Timestamp**: 2026-08-30 16:20
**Source mode**: Refinement (Two-File) — NOT a human edit pair
**Draft (before)**: `ψ/incubate/drafts/crdb-exec-summary-4.3/_archive-wrong-section-mapping/section-4.3-draft.md` (the original AI-verbalized governance/TOR/data-exchange content, committed once at `2e47d12` and never edited — this is what "§4.4" used to contain before the topic outline shifted and this content was renumbered/replaced)
**Edited (after)**: `ψ/incubate/drafts/crdb-exec-summary-4.4/section-4.4-draft.md` (current working-copy content, produced by the revision-mode pipeline's Stage 3 re-verbalization)
**Context**: TH (repo-wide Thai institutional pack)
**Confirmed by user**: Boss confirmed the old §4.4 content became §4.3 when the outline shifted; the two files being compared are pipeline output on both sides, not a human correction of AI prose.

## Scope decision (important — read before reusing this evidence)

The raw `diff_word_table.py` output for this pair returned 138 rows, but the two
files are **not a draft-vs-edited pair of the same passage** — they are two
separate AI-verbalizer outputs for content that got reorganized across a topic-
outline shift (formatting also differs: `###` headings → bold, `*` bullets →
`-`/tab, pipe table → bold-inline table). Treating the full row set as
corrective style patterns would encode arbitrary AI-regeneration variance as if
it were a human style preference, which this skill is not for.

**Boss scoped this capture down to terminology swaps only** — the handful of
role/term relabelings below, which look like a deliberate standardization
rather than incidental rewrite noise. The remaining ~130 rows are NOT
dispositioned individually and are explicitly out of scope for this run.

## Exhaustive Word-by-Word Lexicon Table (terminology-swap subset only)

| Banned/Draft | Preferred/Edit | Category | Institutional Rationale |
| :--- | :--- | :--- | :--- |
| `ผู้ถือครองข้อมูล` (Data Owner gloss) | `เจ้าของข้อมูล` | Institutional Domain Precision | "Holder" (`ถือครอง`) softens the accountable-owner relationship the DAMA-style role implies; `เจ้าของ` (owner) states it directly. |
| `ผู้ดูแลข้อมูล` (Data Steward gloss) | `บริกรข้อมูล` | Institutional Domain Precision | Reframes the Data Steward role from generic caretaking (`ผู้ดูแล`) to an active, standards-conformance service function (`บริกร...คอยดูแลคุณภาพ...ให้เป็นไปตามมาตรฐานที่รองรับโดยคณะกรรมการ`). |
| `Technical Steward` | `Data Custodian` | Domain Framework Alignment | Aligns the English role label to the standard three-tier DAMA-style governance framework (Data Owner / Data Steward / Data Custodian) instead of an ad hoc "Technical Steward" label; the Thai gloss (`ผู้ดูแลด้านเทคนิค`) stayed the same. |
| `Data Exchange Agreement` | `Data Contract` | Domain Framework Alignment | Replaces the generic legal-sounding term with the current data-engineering term of art for a producer/consumer data-sharing agreement. |

## Not dispositioned (out of scope, logged for the record only)

The remaining ~130 diff rows are structural reflow (heading/bullet/table format
changes) and content reorganization (paragraph reordering, expanded MVP/Agile/
Data-Contract substance, product grouping changed from 3+2 with different
membership) tied to the outline shift, not editorial style correction. Not
registered as candidate patterns.

## Candidate Rules

- **L1/L2 Lexical**:
  - `ผู้ถือครองข้อมูล` ➔ `เจ้าของข้อมูล` (Data Owner Thai gloss)
  - `ผู้ดูแลข้อมูล` ➔ `บริกรข้อมูล` (Data Steward Thai gloss)
  - `Technical Steward` ➔ `Data Custodian` (English role label)
  - `Data Exchange Agreement` ➔ `Data Contract` (English term)
  - All four pending rationale-gate confirmation (step 4c) before promotion — not yet mechanical-confirmed.
